# 光谱方块 · TETRA

一个无需构建工具的单文件 HTML 俄罗斯方块游戏。

## 在线运行

仓库启用 GitHub Pages 后，访问 Pages 地址即可直接开始游戏。仓库根目录的 `index.html` 是页面入口。

## 本地运行

直接双击 `index.html` 即可运行，也可以在项目目录启动任意静态文件服务器。

## 操作

- 键盘方向键移动和旋转方块
- `空格` 硬降
- `P` 暂停
- 移动端使用页面内触控按钮
